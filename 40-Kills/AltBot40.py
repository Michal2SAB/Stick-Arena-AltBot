# Since each server except labpass one had the 40 kill limit, there was no point dealing with bad TOR connections anymore. Instead we could spread the alting games across multiple servers, 4 in each.
